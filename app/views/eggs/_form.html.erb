<%= form_for(@egg) do |f| %>
  <% if @egg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@egg.errors.count, "error") %> prohibited this egg from being saved:</h2>

      <ul>
      <% @egg.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
    <%= f.label :user %><br>
    <%= f.hidden_field :user %>
  </div>
  <div class="field">
    <%= f.label :company  %><br>
    <%= f.hidden_field :company %>
  </div>
  <div class="field">
    <%= f.label :tel %><br>
    <%= f.hidden_field :tel%>
  </div>
  <div class="field">
    <%= f.select :location, options_for_select(%w[Bole Yeka kirkos]) %>
  </div>
  <div class="field">
    <%= f.select :type_of_egg, options_for_select(%w[Habesha Ferenj]) %>
  </div>
  <div class="field">
    <%= f.label :daily_quantity %><br>
    <%= f.number_field :daily_quantity %>
  </div>
 
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.select :reciept, options_for_select(%w[VAT TOT None]) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
