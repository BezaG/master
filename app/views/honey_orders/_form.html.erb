<%= form_for([@honey_order.honey, @honey_order]) do |f| %>
  <% if @honey_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@honey_order.errors.count, "error") %> prohibited this honey order from being saved:</h2>

      <ul>
      <% @honey_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    
    <%= f.hidden_field :user_id %>
  </div>
  <div class="field">
    
    <%= f.hidden_field :honey_id %>
  </div>
  <div class="field">
    
    <%= f.hidden_field :company %>
  </div>
 <div class="field">
    <%= f.label :location %>
    <%= f.select :location, options_for_select(%w[Bole Yeka Kirkos Arada Gulele Nifas_Silk_Lafto Addis_Ketema Akaki Ledeta Lafto]) %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
